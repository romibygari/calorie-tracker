{
  "cli": {
    "version": ">= 8.0.0"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "android": {
        "buildType": "apk"
      },
      "distribution": "internal",
      "prebuildCommand": "CI=1 npx expo prebuild --platform android && node -e \"const fs=require('fs'),f='android/gradle/wrapper/gradle-wrapper.properties';fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/gradle-[0-9.]+-all/g,'gradle-8.3-all'))\""
    },
    "production": {
      "android": {
        "buildType": "app-bundle"
      }
    }
  }
}
